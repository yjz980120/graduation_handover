方案可行，但是就是soma_radius和soma_dist参数可能要再确定一下
应该需要一些相关统计数据，来更好的确定参数

现在最粗糙的做法是，检查radius，当一个点的radius>10就认为是soma上的，同时当这个点距离soma中心的距离>100的时候(算距离的时候为了精准，注意分辨率大概是0.3:0.3:1, 也就是z方向要*3，z方向乘了3的话，整体变大了，那这个距离阈值给卡到150？)，就认为是另一个soma的？   但是现在这样肯定有问题，因为有的soma离得近，距离100的这个阈值很不好卡，所以后面soma detection可能需要更有效的方法，但是目前这个方法应该是最简单有效的

